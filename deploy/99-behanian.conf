# Si un task kernel est bloqué >120s → kernel panic (plutôt que freeze silencieux)
kernel.hung_task_panic = 1
kernel.hung_task_timeout_secs = 120
# Reboot automatique 30s après un kernel panic
kernel.panic = 30
# Ne pas paniquer sur OOM (RAM suffisante, swap présent)
vm.panic_on_oom = 0
